# Tredence-Assignment-Self-pruning-neural-network-
A self-pruning neural network that learns to remove unnecessary connections during training. Each weight is paired with a gate, and an L1 sparsity term encourages weaker connections to vanish. Trained on CIFAR-10, the project explores the trade-off between accuracy and model efficiency.
